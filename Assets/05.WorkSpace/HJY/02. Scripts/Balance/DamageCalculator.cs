
//using UnityEngine;

public static class DamageCalculator
{
    //public static float CalculateDamage(EnemyAttackType enemyType)
    //{
    //    float damage = PlayerStatManager.Instance.attack;

    //    foreach (var item in InventoryManager.Instance.equippedItems)
    //    {
    //        if (item == null) continue;
    //        if (item.ringBonus.enemyType == enemyType)
    //        {
    //            damage += item.ringBonus.bonusDamage;
    //        }     
    //    }

    //    return damage;
    //}
}

